# Path to your oh-my-zsh installation.
export ZSH="/home/rey/.oh-my-zsh"

# favorite one theme
#ZSH_THEME="tjkirch_mod"
ZSH_THEME="spaceship"
#ZSH_THEME="robbyrussell"
#ZSH_THEME="random"
#ZSH_THEME="powerlevel10k/powerlevel10k"


plugins=(git zsh-syntax-highlighting zsh-autosuggestions sudo extract web-search vi-mode)

source $ZSH/oh-my-zsh.sh

source ~/.config/zsh/vi.zsh
source ~/.config/zsh/aliaes.zsh
source ~/.config/zsh/env.zsh
source ~/.config/zsh/fzf.zsh
source ~/.config/zsh/plugins.zsh
source ~/.zim/modules/fzf-tab/fzf-tab.plugin.zsh

###
###FZF
###
#[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

#export FZF_DEFAULT_COMMAND='fd --hidden --follow -E ".git" -E "node_modules" . /etc /home'

#export FZF_DEFAULT_OPTS='--height 90% --layout=reverse --bind ctrl-e:down,ctrl-u:up,alt-e:toggle+down --border --preview "echo {} | ~/scripts/fzf_preview.py" --preview-window=down'

#export FZF_COMPLETION_TRIGGER='\'
# Options to fzf command
#export FZF_COMPLETION_OPTS=''
#

# Use fd (https://github.com/sharkdp/fd) instead of the default find
# command for listing path candidates.
# - The first argument to the function ($1) is the base path to start traversal
# - See the source code (completion.{bash,zsh}) for the details.
#_fzf_compgen_path() {
  #fd --hidden --follow -E ".git" -E "node_modules" . /etc /home
#}

## Use fd to generate the list for directory completion
#_fzf_compgen_dir() {
  #fd --type d --hidden --follow -E ".git" -E "node_modules" . /etc /home
#}


